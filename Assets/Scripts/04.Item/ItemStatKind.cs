using UnityEngine;

public enum ItemStatKind
{
    PlusMaxHealth,      // 최대체력 증가
    PlusMaxMana,        // 최대마나 증가
    PlusAttack,         // 공격력 증가
    PlusDefense,        // 방어력 증가
    PlusMoveSpeed,      // 이동속도 증가
    PlusAttackSpeed,    // 공격속도 증가
    PlusCritChance,     // 치명타 확률 증가
    PlusCritDamage,    // 치명타 피해 증가
    PlusHealthRegen,   // 체력 재생 증가
    PlusManaRegen,     // 마나 재생 증가
}

